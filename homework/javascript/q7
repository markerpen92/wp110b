var transport_v
function transpose(v)
{
    
    transport_v = []
    for(let i=0 ; i<v[0].length ; i++)
    {
        for(let j=0 ; j<v.length ; j++)
        {
            console.log(v[j][i])
        }
        console.log("\n")
    }
}

v=[[1,2,3,4],[4,3,2,1],[1,1,1,1]]
console.log(transpose(v))
